import {ADD_PRODUCT} from '../constants';

const initialState = [];

export default function basketReducer(state = initialState, action) {
    switch (action.type) {
        case ADD_PRODUCT:
        //add place,
        default:
            return state
    }
}